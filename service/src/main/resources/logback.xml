<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%level] %logger{36}: %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender -->
    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/srv/zipline/fetcher/logs/zipline-fs.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%level] %logger{36}: %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>/srv/zipline/fetcher/logs/zipline-fs.%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>30</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>100MB</maxFileSize>
        </triggeringPolicy>
    </appender>

    <!-- Root logger with environment variable support -->
    <root level="${LOG_LEVEL:-info}">
        <appender-ref ref="Console" />
        <appender-ref ref="File" />
    </root>

    <!-- Suppress noisy third-party libraries -->
    <logger name="org.apache.spark" level="WARN" />
    <logger name="org.apache.hadoop" level="WARN" />
    <logger name="org.apache.kafka" level="WARN" />
    <logger name="io.netty" level="WARN" />
    <logger name="io.vertx" level="WARN" />
    <logger name="io.micrometer" level="ERROR" />

</configuration>
