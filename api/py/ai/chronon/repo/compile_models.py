
def extract_and_convert(chronon_root, input_path, output_root, debug, force_overwrite, feature_display):
    # TODO: Produce compiled model thrift files
    # Ensure that associated Joins are also compiled 
    return
